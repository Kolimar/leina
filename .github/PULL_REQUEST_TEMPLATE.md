## What does this PR do?

<!-- One or two sentences: the change and why. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes (zero warnings)
- [ ] New behavior (command, flag, fix) has a test in `test/`
- [ ] Docs updated if a command/flag changed (`docs/CLI_REFERENCE.md` and the root help
      in `src/cli/handlers/system.ts`)
